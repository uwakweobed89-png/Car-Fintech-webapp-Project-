// Scroll distance (px) over which the Home intro (IntroLogoMorph) fully
// plays. HomePage uses the same value to size the hero's sticky "runway" so
// the hero stays pinned in view for the whole intro instead of scrolling
// away underneath it.
export const INTRO_SCROLL_DISTANCE = 520;
