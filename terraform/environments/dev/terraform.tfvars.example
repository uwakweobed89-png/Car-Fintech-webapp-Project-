# Copy to terraform.tfvars (gitignored) and fill in real secrets.
# Never commit terraform.tfvars.

db_password = "CHANGE_ME_TO_A_REAL_SECRET"

# Generate with: openssl rand -hex 32
admin_api_key = "CHANGE_ME_TO_A_REAL_SECRET"

# Defaults in variables.tf already match the live CLOUD-OPS-project resources
# (myapp-vpc, cloudops-cluster, fintech-payment-api ECR repo) — override only
# if those names change.
